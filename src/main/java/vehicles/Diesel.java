package vehicles;

import upgrades.Aircon;
import upgrades.Engine;

public class Diesel extends Car{
    public Diesel(String model, int price, String fuelType, String color, int condition) {
        super(model, price, fuelType, color, condition);
    }
}
